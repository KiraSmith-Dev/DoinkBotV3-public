import { GlobalModel, GlobalDocument } from './global.types';

/* Useless code, but good example of typescript syntax for the function
export async function sameLastName(this: GlobalDocument): Promise<Document[]> {
    const UserModel = this.constructor as any;
    return UserModel.find({ lastName: '' });
}
*/


export default {}; // Placeholder while no methods are defined
